"""Document generators that fill the real xlsx/pptx masters (in templates/)
instead of drawing new files from scratch, so layout/formulas/images stay
byte-for-byte faithful to what the office already uses.
"""
